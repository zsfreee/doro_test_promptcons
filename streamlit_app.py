import streamlit as st
import openai
from openai import OpenAI
import time

# Конфигурация страницы
st.set_page_config(
    page_title="DoroMarine AI Консультант",
    page_icon="🐻",
    layout="wide",
    initial_sidebar_state="expanded"
)

# Системный промпт
SYSTEM_PROMPT = '''Ты гениальный AI продажник
Твоя задача провести предварительный этап продажи с клиентом (мной) и довести меня до согласия оставить контакты, чтобы мне позвонил специалист для финальной продажи

Вводная часть
Я оставил заявку на бесплатную консультацию по детскому здоровью, считай, что я родитель и волнуюсь за здоровье своего ребенка
Мы общаемся с тобой в Telegram боте, я в него подписался после заявки

Давай сделаем агентскую систему, действуй всегда от имени конкретного агента.
Я могу попросить "Действуй, как Агент-A".
Твои агенты могут общаться друг с другом и просить тоже действовать, принимай их запросы так же, как мои и выполняй.
Меня будем называть "Клиент".
Запись должна быть в формате, например:
От Агента-А к Агенту-B
От Агента-С к Клиенту
В рамках одного сообщения Агент-А может попросить что-то Агента-B, а тот может обратиться к клиенту - в одном твоём сообщении.
Агент-А и Агент-B - это только примеры названий, в реальности пиши настоящие названия агентов

Команда агентов:

- Агент-консультант: отвечает на вопросы клиента по продукту DoroMarine, обязательно дополняет ответ вопросом, который продвинет продажу вперёд
- Агент-выявления потребностей: задаёт вопросы клиенту о здоровье его ребенка, проблемах, симптомах, его беспокойствах
- Агент-генератор отчета: анализирует, выявили ли мы значимую потребность клиента и закрыли ли все возражения, презентовали ли продукт
- Агент-презентатор: проводит презентацию DoroMarine под потребности клиента, определяет выгоду для клиента от покупки, обязательно дополняет ответ предложением оставить контакты - имя, телефон и email, чтобы с ним связался специалист и они обсудили покупку
- Агент-генератор оффера: предлагает клиенту оставить контакты - имя, телефон и email, чтобы с ним связался специалист и они обсудили покупку DoroMarine
- Агент-закрытия возражений: отвечает на возражения клиента - либо прямыми контр-аргументами, либо задает дополнительные вопросы, чтобы понять природу и причину возражения

Какая должна быть логика продажи:
- первое приветствие: "Привет! Я AI-консультант DoroMarine. Подскажите, что вас беспокоит в здоровье вашего ребенка? Чаще всего родители обращаются из-за проблем с иммунитетом, пищеварением или подозрений на паразитов"
- выявляем потребность - задаем вопросы (агент-выявления потребностей)
- в процессе отвечаем на вопросы клиента, если они есть (агент-консультант)
- проверяем, выявили ли мы все потребности (агент-генератор отчета)
- презентуем DoroMarine под потребности клиента (агент-презентатор)
- предлагаем оставить контакты, чтобы позвонил специалист и продал продукт (агент-генератор оффера)
- закрываем возражения (агент-закрытия возражений)
- повторно предлагаем оставить контакты (агент-генератор оффера)
- повторно закрываем возражения (агент-закрытия возражений)

Как долго выявлять потребности
- Агент-генератор отчёта должен срабатывать каждый раз
- Агент-генератор вопросов должен анализировать отчёт и проверять, выявили ли мы сильную потребность клиента
- Агент-генератор вопросов должен генерировать вопросы для выявления потребности, пока мы не найдём действительно сильную причину у клиента для покупки DoroMarine

Для выявления потребностей надо задать не менее 5 вопросов

Важно: обязательно в каждом твоём ответе должен быть отчёт типа
"Агент-генератор отчёта всем другим Агентам" и дальше отчёт от Агента генератора отчётов
Важные дополнения:
- Агент-генератор отчётов должен всегда писать первым
- Агент-генератор отчётов должен накапливать свой отчёт, то есть всегда добавлять к нему что-то, но не терять те важные выводы, которые он написал ранее

Формат отчёта Агента-генератора отчётов:
- Потребности клиента (проблемы со здоровьем ребенка)
- Возражения клиента
- Общий комментарий
- Важные фразы клиента дословно - не последняя, а вообще все
- Следующий шаг - кто из Агентов что должен сделать

Основные потребности (для агента-выявления потребностей) и выгоды для человека (для агента-презентатора)
- Паразиты и глисты - ребенок заражен паразитами или есть подозрения. Выгоды: мармеладки выводят глистов и их личинки, очищают от токсинов, защищают печень
- Слабый иммунитет - ребенок часто болеет. Выгоды: повышают иммунитет, дают энергию, насыщают витаминами и микроэлементами
- Проблемы с пищеварением - боли в животе, тошнота, проблемы со стулом. Выгоды: улучшают работу ЖКТ, помогают при язве, колите, нормализуют пищеварение
- Проблемы с щитовидной железой - дефицит йода. Выгоды: фейхоа богата усваиваемым йодом, нормализует работу щитовидки
- Интоксикация организма - плохая экология, тяжелые металлы. Выгоды: пектин очищает от солей тяжелых металлов, радионуклидов, холестерина
- Низкий гемоглобин и анемия. Выгоды: яблочный порошок повышает гемоглобин, укрепляет нервную систему
- Проблемы с обменом веществ. Выгоды: ускоряет метаболизм, нормализует работу эндокринной системы

Основные возражения и их закрытие (для агента-закрытия возражений)
- Сколько это стоит? Это дорого - закрытие: стоимость 9700 тенге за курс на месяц, это всего 323 тенге в день для здоровья ребенка, есть рассрочка через Kaspi
- Это не лекарство, зачем мне БАД - закрытие: это натуральный продукт без химии, но перед применением нужна консультация специалиста, который определит подходит ли продукт именно вашему ребенку
- У ребенка аллергия, боюсь давать - закрытие: состав полностью натуральный, но именно поэтому важна консультация со специалистом, чтобы исключить индивидуальную непереносимость
- Ребенок не будет есть, не любит такое - закрытие: это вкусные мармеладные мишки, дети их обожают, принимать легко и приятно
- Мне хватит просто консультации - закрытие: конечно, специалист даст полную информацию и вы сможете взвешенно принять решение
- Зачем мне разговаривать с человеком - закрытие: специалист сможет учесть индивидуальные особенности именно вашего ребенка и дать персональные рекомендации
- Я сначала с врачом посоветуюсь - закрытие: это правильно! Специалист как раз и объяснит, как сочетать с назначениями врача
- У нас нет таких проблем - закрытие: профилактика всегда лучше лечения, и многие проблемы не видны сразу
Всегда добавляй, что для того и нужна консультация, чтобы специалист дал точную информацию именно для вашего случая

Основные тезисы для презентации (для агента-презентатора)
Тезисы о проблеме
- 80% детей имеют паразитов, но диагностика их выявляет только в 20% случаев
- Паразиты снижают иммунитет и мешают усвоению витаминов
- Плохая экология и питание приводят к интоксикации детского организма
- Дефицит йода есть у 70% детей в Казахстане

Тезисы о продукте DoroMarine
- 100% натуральный состав без химии и консервантов
- Для детей от 2 до 14 лет
- Удобная форма - вкусные мармеладные мишки
- Комплексное действие: от паразитов, для иммунитета, для пищеварения
- Сертифицирован Халяль ДУМК
- 60 мармеладок в упаковке - полный курс
- Принимать 1-3 штуки 3-4 раза в день

Тезисы о компонентах
- Пектин - очищает от тяжелых металлов и токсинов
- Сок папайи - улучшает пищеварение, помогает при язве
- Сок горького арбуза - выводит паразитов и их личинки
- Фейхоа - источник усваиваемого йода для щитовидной железы
- Яблочный порошок - повышает гемоглобин, ускоряет метаболизм

Тезисы о компании
- Специализируемся только на детском здоровье
- Натуральные продукты без химии
- Сертификация Халяль
- Доставка по всему Казахстану
- Консультации специалистов
- Гарантия качества

Стилистика общения
Общайся как заботливый детский консультант, без формальностей. Действуй в архетипе Заботливого наставника и Целителя
Говори с пониманием родительских переживаний, добавляй эмоции, часто хвали родителя за заботу о ребенке, подчеркивай важность профилактики

Твоя цель, чтобы продажа прошла для Клиента максимально просто и прозрачно, он не должен пытаться продвигать продажу вперёд, это должен делать ты
Клиент не специалист в промтах и не знает, как тобой управлять

Начинай с приветствия и первого вопроса из плана консультации
Заканчивай разговор после получения контактов и со словом 'Спасибо'.'''

import hmac

@st.cache_resource
def init_openai_client():
    """Инициализация OpenAI клиента с использованием Streamlit secrets"""
    try:
        # Получаем API ключ из secrets (поддерживаем оба формата)
        try:
            api_key = st.secrets.openai.api_key
        except AttributeError:
            api_key = st.secrets["OPENAI_API_KEY"]
        
        client = OpenAI(api_key=api_key)
        return client
    except Exception as e:
        st.error(f"Ошибка инициализации OpenAI: {e}")
        st.error("Убедитесь, что OPENAI_API_KEY добавлен в Streamlit secrets")
        return None

def check_password():
    """Returns True if the user entered the correct password."""
    if "password_correct" in st.session_state:
        return st.session_state.password_correct
    
    def password_entered():
        """Checks whether a password entered by the user is correct."""
        try:
            # Поддерживаем оба формата secrets
            try:
                password = st.secrets.auth.password
            except AttributeError:
                password = st.secrets["PASSWORD"]
            
            if hmac.compare_digest(
                st.session_state.password,
                password
            ):
                st.session_state.password_correct = True
                del st.session_state.password  # Не храним пароль в сессии
            else:
                st.session_state.password_correct = False
        except Exception as e:
            st.error(f"Ошибка проверки пароля: {e}")
            st.session_state.password_correct = False
    
    # Оформление страницы входа
    st.markdown("### 🔐 Вход в систему DoroMarine AI")
    st.markdown("*Введите пароль для доступа к AI-консультанту*")
    
    # Форма входа
    with st.form("login_form"):
        st.text_input(
            "Пароль для доступа к приложению:",
            type="password",
            key="password",
            placeholder="Введите пароль доступа"
        )
        login_button = st.form_submit_button("🚪 Войти", on_click=password_entered)
    
    # Проверка результата
    if "password_correct" in st.session_state:
        if not st.session_state.password_correct:
            st.error("😕 Неверный пароль")
            return False
    
    # Информация для администратора
    with st.expander("ℹ️ Информация для администратора"):
        st.markdown("""
        **Доступ к AI-консультанту защищен паролем.**
        
        Для получения пароля обратитесь к администратору системы.
        
        📧 Email: admin@doromarine.kz  
        📞 Телефон: +7 (XXX) XXX-XX-XX
        
        🔒 **Безопасность:** Используется защищенная проверка пароля с HMAC
        """)
    
    return False

def chat_with_memory(client, history, system_prompt, user_message):
    """Функция общения с AI с сохранением памяти"""
    try:
        # Формируем полную историю
        messages = [{"role": "system", "content": system_prompt}]
        
        # Добавляем историю сообщений
        for msg in history:
            if msg["role"] in ["user", "assistant"]:
                messages.append(msg)
        
        # Добавляем текущее сообщение пользователя
        if user_message:
            messages.append({"role": "user", "content": user_message})
        
        # Запрос к OpenAI
        response = client.chat.completions.create(
            model="gpt-4o-mini",
            messages=messages,
            temperature=0.15,
            max_tokens=1500
        )
        
        return response.choices[0].message.content
        
    except Exception as e:
        return f"Произошла ошибка при обращении к AI: {e}"

def split_response_with_gpt(client, response):
    """Разделение ответа на видимую и скрытую части"""
    try:
        response_obj = client.chat.completions.create(
            model="gpt-4o-mini",
            messages=[
                {"role": "system", "content": """Ты помощник, который разделяет текст на две части: первую (видимую) часть для пользователя и вторую (скрытую) часть для внутренней обработки.
                Пожалуйста, оставь в первой части только информацию, которую можно показать пользователю, где Агент обращается к Клиенту, а во второй — информацию, которая не должна
                быть показана - это внутренние переговоры агентов.
                Указывай каждую часть с заголовком: **Видимая часть для пользователя:** и **Скрытая часть для внутренней обработки:**"""},
                {"role": "user", "content": f"Раздели следующий ответ: {response}"}
            ],
            temperature=0.15,
        )
        
        split_answer = response_obj.choices[0].message.content.strip()
        
        # Разделяем на части
        visible_marker = "**Видимая часть для пользователя:**"
        hidden_marker = "**Скрытая часть для внутренней обработки:**"
        
        visible_part = ""
        hidden_part = ""
        
        if visible_marker in split_answer and hidden_marker in split_answer:
            visible_part = split_answer.split(visible_marker)[1].split(hidden_marker)[0].strip()
            hidden_part = split_answer.split(hidden_marker)[1].strip()
        elif visible_marker in split_answer:
            visible_part = split_answer.split(visible_marker)[1].strip()
        else:
            visible_part = response  # Если разделение не удалось, показываем весь ответ
            
        return visible_part, hidden_part
        
    except Exception as e:
        return response, f"Ошибка разделения: {e}"

def main():
    st.title("🐻 DoroMarine AI Консультант")
    st.markdown("*Забота о здоровье вашего ребенка - наш приоритет*")
    
    # Проверка пароля - если не прошел, останавливаем выполнение
    if not check_password():
        st.stop()
    
    # Показываем информацию об успешном входе в sidebar
    with st.sidebar:
        st.success("✅ Успешная авторизация")
        st.markdown(f"**Пользователь:** Администратор")
        
        if st.button("🚪 Выйти из системы", use_container_width=True):
            # Очищаем данные авторизации и сессии
            for key in list(st.session_state.keys()):
                del st.session_state[key]
            st.rerun()
        
        st.markdown("---")
    
    # Инициализация OpenAI клиента
    client = init_openai_client()
    
    if not client:
        st.stop()
    
    # Боковая панель с информацией
    with st.sidebar:
        st.header("ℹ️ О консультации")
        
        st.markdown("""
        **DoroMarine** - натуральные витамины для детского здоровья
        
        🎯 **Что мы обсуждаем:**
        • Проблемы с иммунитетом
        • Вопросы пищеварения
        • Профилактика паразитов
        • Общее здоровье ребенка
        
        🔒 **Конфиденциально и безопасно**
        """)
        
        st.markdown("---")
        
        # Статистика сессии
        if 'messages' in st.session_state:
            st.metric("Сообщений в диалоге", len(st.session_state.messages))
        
        # Кнопка очистки чата
        if st.button("🗑️ Начать заново", use_container_width=True):
            st.session_state.messages = []
            st.session_state.chat_history = []
            st.session_state.conversation_started = False
            st.rerun()
        
        # Информация о продукте
        with st.expander("📋 О DoroMarine"):
            st.markdown("""
            **Состав:** Пектин, сок папайи, сок горького арбуза, фейхоа, яблочный порошок
            
            **Форма:** Мармеладные мишки
            
            **Возраст:** 2-14 лет
            
            **Курс:** 60 штук на месяц
            
            **Цена:** 9700 тенге
            
            ✅ Сертификат Халяль ДУМК
            """)
    
    # Инициализация сессии
    if 'messages' not in st.session_state:
        st.session_state.messages = []
    
    if 'chat_history' not in st.session_state:
        st.session_state.chat_history = []
    
    if 'conversation_started' not in st.session_state:
        st.session_state.conversation_started = False
    
    # Автоматический запуск консультации
    if not st.session_state.conversation_started:
        with st.spinner("🤖 Подключаем AI-консультанта..."):
            time.sleep(1)  # Небольшая задержка для реалистичности
            initial_response = chat_with_memory(client, st.session_state.chat_history, SYSTEM_PROMPT, "")
            
            if initial_response and "Произошла ошибка" not in initial_response:
                visible_part, hidden_part = split_response_with_gpt(client, initial_response)
                
                # Добавляем в историю
                st.session_state.messages.append({
                    "role": "assistant", 
                    "content": visible_part,
                    "timestamp": time.time()
                })
                st.session_state.chat_history.append({"role": "assistant", "content": initial_response})
                
                st.session_state.conversation_started = True
                st.rerun()
            else:
                st.error("❌ Не удалось подключить консультанта. Попробуйте обновить страницу.")
    
    # Отображение истории чата
    chat_container = st.container()
    with chat_container:
        if st.session_state.messages:
            for i, message in enumerate(st.session_state.messages):
                with st.chat_message(message["role"]):
                    if message["role"] == "assistant":
                        st.markdown(f"🤖 **Консультант DoroMarine:**")
                        st.markdown(message['content'])
                    else:
                        st.markdown(f"👤 **Вы:**")
                        st.markdown(message['content'])
        else:
            # Показываем приветственное сообщение пока AI не подключился
            st.info("🔄 Инициализация консультации...")
    
    # Поле ввода сообщения
    if st.session_state.conversation_started:
        user_input = st.chat_input("Напишите ваше сообщение...")
        
        if user_input:
            # Добавляем сообщение пользователя
            st.session_state.messages.append({
                "role": "user", 
                "content": user_input,
                "timestamp": time.time()
            })
            st.session_state.chat_history.append({"role": "user", "content": user_input})
            
            # Генерируем ответ AI
            with st.spinner("🤔 Консультант анализирует..."):
                ai_response = chat_with_memory(client, st.session_state.chat_history, SYSTEM_PROMPT, user_input)
                
                if ai_response and "Произошла ошибка" not in ai_response:
                    # Разделяем ответ
                    visible_part, hidden_part = split_response_with_gpt(client, ai_response)
                    
                    # Добавляем видимую часть в интерфейс
                    st.session_state.messages.append({
                        "role": "assistant", 
                        "content": visible_part,
                        "timestamp": time.time()
                    })
                    
                    # Сохраняем полный ответ в историю для AI
                    st.session_state.chat_history.append({"role": "assistant", "content": ai_response})
                    
                    # Проверяем на завершение разговора
                    if "Спасибо" in ai_response and any(keyword in user_input.lower() for keyword in ["контакт", "телефон", "email", "@", "+7", "+8"]):
                        st.balloons()
                        st.success("🎉 Консультация завершена! Наш специалист свяжется с вами в ближайшее время.")
                        
                        # Показываем финальную информацию
                        with st.expander("📞 Что дальше?", expanded=True):
                            st.markdown("""
                            ✅ **Ваши контакты получены**
                            
                            📞 Наш специалист свяжется с вами в течение 24 часов
                            
                            🕐 Время работы: ПН-ПТ с 9:00 до 18:00
                            
                            📧 По вопросам: info@doromarine.kz
                            """)
                else:
                    st.error("❌ Произошла ошибка при получении ответа. Попробуйте еще раз.")
            
            st.rerun()
    
    # Футер
    st.markdown("---")
    st.markdown(
        "<div style='text-align: center; color: #666; font-size: 12px;'>"
        "DoroMarine © 2024 | Натуральные витамины для детей"
        "</div>", 
        unsafe_allow_html=True
    )

if __name__ == "__main__":
    main()
